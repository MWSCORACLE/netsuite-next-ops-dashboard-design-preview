# NetSuite Next Operating Dashboard Design Preview

This folder is a sanitized design preview for sharing the dashboard UI. It uses synthetic data and placeholder source links only.

The live internal dashboard remains in `consolidated-dashboard`.
